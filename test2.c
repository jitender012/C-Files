#include <stdio.h>
#include <conio.h>

// Comparison of two strings
/*int main(int argc, char const *argv[])
{
    char name1[20],  name2[20];
    int value;
    printf("Enter your first name:\n");
    gets(name1);
    printf("Enter your last name:\n");
    gets(name2);
    value=strcmp(name1,name2);
    if(value==0)
            puts("Both names are equal");
        else if (value<0)
            puts("The first name is smaller");
        else if (value>0)
            puts("The first name is bigger");



    return 0;
}
//print 3 characters
int main(int argc, char const *argv[])
{
    char name[]="jitender";
    printf("%-8.3s \n",name);
    return 0;
}
____________________________________________


int main(int argc, char const *argv[])
{
    char name[]="RAM";
    int i, j, l;
    l=strlen(name);
    for ( i = 0; i < l; i++)
    {
        for ( j = 0; j <= i; j++)
        {
            printf("%c",name[j]);
        }

        printf("\n");

    }

    return 0;
}
____________________________________________



#include <stdio.h>
int main()
    {
        void sample();
        printf("\n You are in main");

    }

        void sample()
    {
        printf("\n You are in sample");
    }
____________________________________________

void max(int a, int b);         // function prototype
void main()
{
    max(5, 23);
}
void max(int a, int b)          // function definition
{
    if (a > b)
        printf("%d is greater than %d\n\n", a, b);
    else
        printf("%d is smaller than %d\n\n", a, b);
}


____________________________________________*/
/*
// program to calculate square of an integer using the function prototype
int main()
{
    int n, sq;
    int square(int no); // function prototype
    printf("Enter a number to calculate square value:\n");
    scanf("%d", &n);
    sq = square(n); // function with parameter passing
    printf("\n Square of the number %d is: %d\n\n", n, sq);
}
// square function
int square(int no) // passing of argument
{
    int result;
    result = no * no;
    return (result);
}
____________________________________________*/
int main(int argc, char const *argv[])
{
    
    int sample(int a, int b);
    printf("%d",n)
    
}
int sample(int n)
{
    if (a>b)
        return(a)
    else
        return(b)    
    
}
